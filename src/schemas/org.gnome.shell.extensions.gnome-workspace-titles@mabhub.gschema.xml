<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <enum id="org.gnome.shell.extensions.gnome-workspace-titles@mabhub.display-mode">
    <value nick="default" value="0"/>
    <value nick="scroll" value="1"/>
    <value nick="overview" value="2"/>
  </enum>

  <schema id="org.gnome.shell.extensions.gnome-workspace-titles@mabhub"
          path="/org/gnome/shell/extensions/gnome-workspace-titles/">

    <key name="panel-box" type="s">
      <default>'left'</default>
      <summary>Panel box position</summary>
      <description>Which panel box to place the indicator in: 'left', 'center', or 'right'.</description>
    </key>

    <key name="panel-position" type="i">
      <default>1</default>
      <summary>Position index in the panel box</summary>
      <description>Index position within the chosen panel box (0 = start). Defaults to 1 so the indicator sits after the Activities button, keeping the 0×0 hot corner clickable.</description>
    </key>

    <key name="rename-shortcut" type="as">
      <default><![CDATA[['<Super>F2']]]></default>
      <summary>Rename current workspace shortcut</summary>
      <description>Accelerator that opens the single-line editor to rename the current workspace.</description>
    </key>

    <key name="rename-shortcut-enabled" type="b">
      <default>true</default>
      <summary>Enable the rename shortcut</summary>
      <description>When true, the rename-current-workspace keybinding is registered.</description>
    </key>

    <key name="edit-all-shortcut" type="as">
      <default><![CDATA[['<Super>F3']]]></default>
      <summary>Edit all workspace names shortcut</summary>
      <description>Accelerator that opens the multiline editor for all workspace names.</description>
    </key>

    <key name="edit-all-shortcut-enabled" type="b">
      <default>true</default>
      <summary>Enable the edit-all shortcut</summary>
      <description>When true, the edit-all-workspace-names keybinding is registered.</description>
    </key>

    <key name="hide-shortcut" type="as">
      <default><![CDATA[['<Super>F4']]]></default>
      <summary>Hide current workspace name shortcut</summary>
      <description>Accelerator that moves the current workspace name into the sorted hidden block.</description>
    </key>

    <key name="hide-shortcut-enabled" type="b">
      <default>true</default>
      <summary>Enable the hide shortcut</summary>
      <description>When true, the hide-current-workspace-name keybinding is registered.</description>
    </key>

    <key name="display-mode" enum="org.gnome.shell.extensions.gnome-workspace-titles@mabhub.display-mode">
      <default>'default'</default>
      <summary>Panel display mode</summary>
      <description>How workspace names are shown in the panel: 'default' (active only), 'scroll' (previous/current/next), or 'overview' (all names).</description>
    </key>

    <key name="overview-max-width" type="i">
      <default>600</default>
      <summary>Overview mode maximum width</summary>
      <description>Maximum width in pixels of the names row in overview mode before it scrolls, keeping the current workspace centered.</description>
    </key>

  </schema>
</schemalist>
